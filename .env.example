# Copie para .env na raiz do repositório: cp .env.example .env
#
# URL pública 
APP_URL=https://github.cedis.tec.br
NEXT_PUBLIC_APP_URL=https://github.cedis.tec.br

# PostgreSQL 16 no host (não roda neste Compose)
DB_HOST=host.docker.internal
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=defina_uma_senha_segura
DB_NAME=tcc_gamification
